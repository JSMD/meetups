// Generated by CoffeeScript 1.12.2
(function() {
  var addPresent, christmasTree, congratulate, presents, showFavoritePresents,
    slice = [].slice;

  congratulate = function() {
    var child, children, i, len, results;
    children = 1 <= arguments.length ? slice.call(arguments, 0) : [];
    results = [];
    for (i = 0, len = children.length; i < len; i++) {
      child = children[i];
      results.push(alert('Merry Christmas ' + child + '!'));
    }
    return results;
  };

  congratulate('Mike', 'Nick', 'James');

  christmasTree = {
    star: 'red',
    garland: true,
    light: (function(_this) {
      return function() {
        if (_this.garland) {
          alert('Shine Christmas tree!');
        }
        _this.star = 'yellow';
      };
    })(this)
  };

  christmasTree.light();

  presents = [];

  addPresent = function(present) {
    if (present == null) {
      present = 'chocolate';
    }
    return presents.push(present);
  };

  addPresent('bicycle');

  addPresent();

  showFavoritePresents = function(firstPresent, secondPresent) {
    return alert('James favorite presents this year are ' + firstPresent + ' and ' + secondPresent);
  };

  showFavoritePresents.apply(null, presents);

}).call(this);

//# sourceMappingURL=arrow-functions.js.map
